# Normalize repository text files to LF across platforms.
* text=auto eol=lf

# Canonical Agent Asset workspace
src/rulesync/** rulesync-source

# Derived cross-runtime discovery metadata
route/*.jsonl linguist-generated
