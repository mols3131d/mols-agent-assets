# Rulesync Agent Assets Tests

These tests protect only the Skill's stable routing contract:

- canonical source uses `generate`;
- native harness source uses `convert`;
- writes are previewed;
- source ownership is preserved;
- custom wrappers remain YAGNI until a concrete backend gap exists.

Backend CLI behavior belongs to Rulesync and is not re-tested here.
