[project]
name = "mols-markdown-tooling"
version = "0.1.0"
description = "Deterministic Markdown maintenance utilities"
readme = "SKILL.md"
requires-python = ">=3.13"
dependencies = [
  "pyromark>=0.9.13",
  "pyyaml>=6.0.2",
]
