version: 1
validation:
  local_commands:
    - id: "skill-tests"
      argv: ["python", "-m", "pytest", "-q", "tests/skills"]
      cwd: "."
      timeout_sec: 120
      required: true
  ci_lanes:
    - id: "agent-assets"
      required: false
