# Iteration Log

1. Audited the legacy CSV router, micro-workflows, validators, optimizers, and
   scriptification workflow.
1. Rebuilt Studio around lifecycle modes, write boundaries, progressive
   disclosure, general review, adversarial review, and truthful outcomes.
1. Created Asset Tuner for project-native adaptation of external assets and
   agent-behavior documentation.
1. Ran an initial general review and added multi-asset validation, explicit
   runtime scaffolds, bounded inventory, overlap triage, and project profiling.
1. Ran an adversarial review and closed symlink, secret filename, YAML injection,
   path leakage, imported-code, and trigger-collision paths.
1. Ran an independent review that returned `Revise` with findings F-001 through
   F-008.
1. Added operation discovery, runtime-specific schema validators, strict package
   validation, likely plaintext-secret scanning, 20+ trigger suites, profile
   discovery, mixed bundle packaging, and reproducible review evidence.
1. Dogfooded the package and fixed secret-scanner self-matching and bundle output
   source-boundary contamination.
1. Passed 27 deterministic regression tests, strict validations, compilation,
   individual-skill packaging, and mixed-bundle packaging.
1. Kept live target-runtime behavior truthfully `Deferred`.

## v2.1 — Legacy Knowledge Absorption

1. Added the `consolidate` mode and explicit Merge, Compose, Route, Keep separate,
   and Deprecate decisions.
1. Added semantic invariants for behavior-preserving compression and refactoring.
1. Added structural hygiene checks for nested skills, symlinks, cache paths, empty
   directories, zero-byte resources, orphan candidates, and undiscovered scripts.
1. Made skill and bundle packages byte reproducible for identical source and toolchain.
1. Added project-first naming, scriptification ROI, conditional rollback, and
   safe host-validator execution.
1. Closed adversarial findings for invariant symlink escape, host-output secret
   leakage, and packaging with unresolved structural warnings.
1. Passed 40 regression tests; live runtime behavior remains Deferred.
