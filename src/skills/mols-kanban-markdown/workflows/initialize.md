# Workflow: Initialize Kanban

## Goal

Set up and initialize a new Markdown Kanban board directory structure with standard configuration, default guide files, and directories.

## Parameters

- **Kanban Path**: Target directory where the Kanban board will be initialized.

## Procedure

1. Execute `scripts/initialize.py` passing the kanban path.
2. Read output and report initialization status.
