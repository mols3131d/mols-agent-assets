# 개발 워크스페이스 (`src/`)

AI 에이전트 자산을 신규 생성, 수정, 실험 및 테스트하는 기본 개발 공간입니다.

---

## 지침

- **구조**: 작업 단위별 하위 디렉터리를 생성하여 개발합니다.
- **검증**: 신규 자산 작성 시 `tests/` 디렉터리에 테스트를 작성합니다.
- **승격**: 검증 완료된 자산은 `release/` 폴더로 승격합니다:

  ```bash
  uv run python scripts/src_to_release.py skills/<skill-name>
  ```
