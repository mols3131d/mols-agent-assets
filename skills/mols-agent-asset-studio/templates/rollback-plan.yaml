version: 1
workspace: "git | non-git"
targets: []
recovery:
  method: "branch | commit | stash | bounded-snapshot"
  reference: "<branch, commit, stash, or snapshot path>"
  command: "<non-destructive recovery command>"
cleanup:
  condition: "<when temporary recovery material can be removed>"
verified: false
