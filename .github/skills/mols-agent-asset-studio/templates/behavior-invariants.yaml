version: 1
required_paths:
  - "SKILL.md"
files:
  SKILL.md:
    literal_strings:
      - "<exact command, path, API name, version, or safety rule>"
    headings:
      - "# <required heading>"
    regexes: []
    ordered_strings:
      - "<first required step>"
      - "<later required step>"
