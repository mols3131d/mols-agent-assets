{
  "version": 2,
  "kind": "deterministic-regression",
  "contract": "chatbot-harness-compatibility",
  "sources": {
    "standard": "docs/references/common/standards/chatbot-repository-bootstrap.md",
    "github_loader": "src/rulesync/.rulesync/skills/load-context-github/SKILL.md",
    "mols_loader": "src/rulesync/.rulesync/skills/load-context-github-mols/SKILL.md"
  },
  "required": {
    "standard": [
      "compatibility responsibility는 독립적으로 적용한다",
      "Partial harness support는 정상적인 상태다",
      "`CHATBOT.md` 자체는 이 hierarchy의 한 단계가 아니다"
    ],
    "github_loader": [
      "Do not classify the caller as chatbot versus coding agent",
      "A repository-defined compatibility/bootstrap entry may route discovery, but it is instruction or policy authority only when the repository explicitly assigns that role",
      "Do not infer that files with different names form a fallback chain",
      "read only selected Skill sources and required supporting resources",
      "match known target paths against declared glob/path/selectors",
      "Do not recursively re-select this loader or another routing asset merely because it appears in a discovered catalog"
    ],
    "mols_loader": [
      "Treat each compatibility responsibility independently",
      "Partial harness support is valid",
      "Do **not** invent `CHATBOT.md → AGENTS.md → README.md` or another filename fallback chain"
    ]
  },
  "forbidden": {
    "mols_loader": [
      "### Instruction Fallback",
      "Unless the repository explicitly defines a different applicable instruction policy"
    ]
  }
}
