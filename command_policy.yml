deny:
  [format, diskpart, Set-ExecutionPolicy, Invoke-Expression, iex, takeown, net]
ask:
  [
    reg,
    Remove-Item,
    del,
    rmdir,
    rm,
    icacls,
    taskkill,
    Stop-Process,
    kill,
    Start-Process,
    saps,
  ]
allow: []
